<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.express.mapper.MessageMapper">
<select id="selectNewMessages" parameterType="Integer" resultType="Message">
select * from message where (to_id=#{value} or from_id=#{value}) and isread='false' order by m_time
</select>
</mapper>